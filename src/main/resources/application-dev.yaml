# Dev profile overrides.
# Session configuration is in initiator.cfg (working directory or classpath).
# Place a custom initiator.cfg next to the jar (or project root for gradlew bootRun)
# to override the bundled default without rebuilding.
