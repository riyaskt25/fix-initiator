spring:
  application:
    name: fix-initiator

fix:
  initiator:
    heartbeat-interval-seconds: 30
    reconnect-interval-seconds: 5
    reset-store-on-start: false
    # Session configuration for connecting to FIX acceptor
    # The initiator will receive orders from the acceptor
    sessions: []
